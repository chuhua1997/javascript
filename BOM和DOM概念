BOM & DOM 
=====================================================================
是由瀏覽器執行環境所提供，在 node 環境下的 JavaSCript 就不會有這兩個部分
前端開發者就是透過 JavaScript 去呼叫 BOM 與DOM提供的 API，
進一步透過它們去控制瀏覽器的行為與網頁的內容。
=====================================================================
